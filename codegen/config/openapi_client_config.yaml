# openapi-python-client config (see https://github.com/openapi-generators/openapi-python-client)
package_name_override: congressgov._client
# Formatting/import order is applied after promote via normalize_client_tree (repo config).
post_hooks: []
